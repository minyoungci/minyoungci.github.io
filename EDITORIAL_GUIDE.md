# 글 발행 편집 가이드라인 (Editorial Guide)

The Gradient에 발행되는 모든 글(직접 작성, DailyAIR import 포함)에 적용하는 규정.
발행 전 이 문서의 체크리스트를 통과해야 한다.

## 1. 문단 (Paragraph)

- **한 문단 = 한 논지.** 논지가 바뀌면 문단을 나눈다.
- 길이 기준: **3~5문장, 250~500자**. 700자를 넘는 문단은 반드시 분할한다.
- 섹션(`##` 헤딩) 하나에 문단 2~4개가 기본. **헤딩 + 단일 거대 문단 금지.**
- 문단을 나눌 때 문장은 그대로 두고 경계만 나눈다 (내용 수정과 분할 작업을 섞지 않는다).

## 1.5 문장 부호와 기호 (자연스러운 글)

- **'—'(em dash), '--', '·' 남용 등 어색한 기호 금지.** 삽입구는 쉼표나 괄호로, 나열은 쉼표로 쓴다.
- 제목도 기호로 잇지 않는다: "제목 — 부제" 형식 대신 쉼표로 잇거나 한 문장으로 자연스럽게 짓는다.
- 허용: 마침표, 쉼표, 물음표, 괄호, 따옴표, 퍼센트·달러 같은 단위 기호, 아라비아 숫자.
- 원문(DailyAIR)에 '—'가 있으면 편집 단계에서 반드시 자연스러운 문장부호로 바꾼다.

## 2. 섹션 구조

- **도입부(리드)**: 헤딩 없이 2~3문단. "무엇이 일어났나 → 왜 지금 중요한가 → 이 글에서 다룰 것".
- **본문**: `## 번호. 제목 — 부제` 형식.
- **마무리**: `## 내가 보는 의미`(필자 견해) → "앞으로 지켜보고 싶은 것"(번호 목록).
- 부록: `## 취재 노트` → `## 참고 문헌` 순서로 글 맨 끝에.

## 3. 출처·검증 메타 정보 (취재 노트)

- 취재 경위, 검증 방법, 확신도, 한계는 본문에 흩어놓지 않고 글 끝 `## 취재 노트` **한 섹션으로 통합**한다.
- 형식: 리드 1문단(출발점·경위) + 불릿 목록(확인 방식과 한계).
- 같은 메타 내용을 본문·인용블록·참고문헌에 **중복 서술하지 않는다.** 메타 서술은 전체 분량의 15% 이내.
- 수치·주장 확신도는 3단계로 명시: **높은 확신도 / 낮은 확신도 / 채택하지 않음**.

## 4. 참고 문헌

- 글 맨 끝 `## 참고 문헌`, 번호 목록.
- 형식: `제목 - 출처/저자, 날짜. 링크` (구분자는 하이픈, em dash 금지)
- 직접 열람하지 못한 자료는 끝에 **(원문 미열람)** 표기.
- 뉴스레터 등 링크가 없는 자료는 수신 정보(매체명, 수신일)만 적는다. 발신 이메일 주소·수신 시각(UTC) 같은 세부는 적지 않는다.

## 5. 이미지

- **커버 1장**(frontmatter `image`) + **본문 삽화 4~5장**. 글의 주요 섹션과 문단마다 내용을 은유하는 그림을 배치한다.
- 톤: 흑백, 잉크 라인, 에디토리얼/인쇄 카탈로그 스타일 — 사이트의 V–A–C 디자인과 통일. 이미지 안에 텍스트·글자 없을 것.
- 위치: 섹션 헤딩 바로 아래. 커버 이미지를 본문에 중복 삽입하지 않는다.
- 저장: `public/images/posts/<slug>/`에 파일로 커밋 (외부 핫링크 금지).
- alt 텍스트는 그림 내용을 설명하는 한 문장.

## 6. Frontmatter

- `title`: 원문 제목 유지 (— 부제 포함 가능)
- `date`: 슬러그 앞의 YYYY-MM-DD
- `tag`: Science | Medical | AI | Finance 중 하나 (DailyAIR 매핑: ai-papers→AI, medical→Medical, economy→Finance)
- `summary`: 도입 문단에서 추출한 100~150자. 취재 경위·메타 서술 금지.
- `image`: 커버 경로 (`/images/posts/<slug>/cover.png`)

## 7. 인용 블록 (blockquote)

- **300자를 넘는 인용 블록 금지** — 긴 메타 서술은 취재 노트로 옮긴다.
- 예외: 짧은 면책조항(의료·투자·정치 중립)은 본문 상단 또는 하단에 1문단 인용 블록으로 유지한다.

## 8. 발행 전 체크리스트

- [ ] 700자 초과 문단 없음
- [ ] "헤딩 + 단일 문단" 섹션 없음
- [ ] 출처·검증 서술이 취재 노트 한 곳에만 있음
- [ ] 참고 문헌 형식 통일 + 미열람 표기
- [ ] 커버·삽화가 저장소 로컬 파일이고 alt 텍스트 있음
- [ ] summary가 본문 요지 (메타 서술 아님)
- [ ] 로컬 프리뷰(dev 서버)에서 렌더링 확인
